#include <iostream>
#include <algorithm>
#include <vector>


int main(){
    int n, curr;
    std::cin >> n;
    std::vector<int> v;
    for(int i = 0; i < n; i++){
        std::cin >> curr;
        v.push_back(curr);
    }
    do{

    }while{}
}