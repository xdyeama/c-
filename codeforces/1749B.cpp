#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    int numCases, n, a, b, time = 0, k = 0; 
    vector<pair<int, int>> v;
    cin >> numCases;
    for(int i = 0; i < numCases; i++){ 
        cin >> n;
        for(int j = 0; j < n; i++){
            cin >> a >> b;
            v.push_back(make_pair(a, b));
        }
        while(v.size() > 0){

        }

    }

    return 0;
}